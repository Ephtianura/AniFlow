import CreateStudioForm from "./CreateStudioForm";

export const metadata = {
    title: "Створення студії | AniFlow"
};

export default function CreateStudioPage() {
    return (
        <CreateStudioForm />
    );
}
